apiVersion: v1
kind: Service
metadata:
  name: jenkins-service
  namespace: devops
spec:
  selector:
    app: jenkins
  ports:
    - name: jenkins-ui
      protocol: TCP
      port: 6666
      targetPort: 8080
    - name: jenkins-slave
      port: 50000
      protocol: TCP
      targetPort: 50000
